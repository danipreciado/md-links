const mdLinks = require('../src/index.js');



describe('mdLinks', () => {
  // Test mdLinks
  describe('mdLinks', () => {
    it('should be a function', () => {
      expect(typeof mdLinks).toBe('function');
    });

})
});
